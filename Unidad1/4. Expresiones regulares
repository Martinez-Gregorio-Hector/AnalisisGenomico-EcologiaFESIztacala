# Expresiones regulares

Una expresión regular es una plantilla de patrón que se define y que una utilidad de Linux utiliza para filtrar texto.

Una utilidad de Linux (como el editor sed o el programa gawk) compara el patrón de la expresión regular con los datos a medida que estos fluyen hacia la utilidad. 
Si los datos coinciden con el patrón, se aceptan para su procesamiento. Si no, se rechazan.

El patrón de expresión regular utiliza caracteres comodín para representar uno o más caracteres en el flujo de datos. 
En Linux, existen numerosos casos en los que se puede especificar un carácter comodín para representar datos desconocidos. 

## Tipos de expresiones regulares

El mayor problema con el uso de expresiones regulares es que no existe un único conjunto. 
Diversas aplicaciones utilizan distintos tipos de expresiones regulares en el entorno Linux. Estas incluyen aplicaciones tan diversas como lenguajes de programación (Java, Perl y Python), 
utilidades de Linux (como el editor sed, el programa gawk y la utilidad grep) y aplicaciones comunes (como los servidores de bases de datos MySQL y PostgreSQL).

Una expresión regular se implementa mediante un motor de expresiones regulares. Un motor de expresiones regulares es el software subyacente que interpreta los patrones de expresiones regulares y 
los utiliza para encontrar coincidencias de texto. En el mundo Linux existen dos motores de expresiones regulares populares:
